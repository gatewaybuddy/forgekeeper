# forgekeeper-core

Core Python agent for Forgekeeper. Provides the `forgekeeper` CLI entrypoint and the agent pipeline.

Install (editable):

```
pip install -e .
```

Run:

```
forgekeeper pipeline
```

Extras:
- `dev`: testing (`pytest`)
- `transformers`: optional local Transformers model support

