# forgekeeper-inference-client

Thin Python client for calling an OpenAI-compatible gateway and rendering Harmony prompts.

Import:

```
from forgekeeper.inference_backends import OpenAICompatClient, select_model, render_harmony
```

Install (editable):

```
pip install -e .
```

